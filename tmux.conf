set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set-option -sg escape-time 10
set -g mouse on

# Vi-style keybindings
setw -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

# Summon/banish keybindings for handling persistent AI tools in the next window
bind-key h join-pane -h -s :+1.0
bind-key v join-pane -v -s :+1.0
bind-key b break-pane -d -t '+1'

# Smart pane switching with awareness of Vim splits. (Modified to remove C-\ handling, I don't need it.)
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
# bind-key -n 'C-w-s' if-shell "$is_vim" 'send-keys C-w s'  'split-window -v -c "#{pane_current_path}"'
# bind-key -n 'C-w-v' if-shell "$is_vim" 'send-keys C-w v'  'split-window -h -c "#{pane_current_path}"'

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R

# Easier splits (not the same as Vim, but pretty good still)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

#
# # Searching through command history with Vim-style bindings
# bind -n C-p previous-window
# bind -n C-n next-window

# Sepia status bar (warm orange‑yellow from the palette)
set -g status-style "bg=#B4974F,fg=#1B1410"      # aged-brass bg, espresso text

# Optional: cohesive highlights/messages
set -g window-status-style "bg=#2B221B,fg=#B4A897"
set -g window-status-current-style "bg=#B96B3D,fg=#FBF1DE,bold"  # cognac current window
set -g message-style "bg=#97633A,fg=#FBF1DE"
set -g message-command-style "bg=#97633A,fg=#FBF1DE"
